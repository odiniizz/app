import { StyleSheet } from "react-native"

export const colors = {
    preto: 'rgb(0, 0, 0)',
    branco: 'rgb(255, 255, 255'

}

export const styles = StyleSheet.create({
    textoTitulo: {
        color: colors.preto,
        fontSize: 75,
        textAlign: 'center',
    },

    textoBotao: {
        
    }
})